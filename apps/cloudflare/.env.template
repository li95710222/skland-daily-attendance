# Cloudflare Workers 环境变量配置模板
# 复制需要的变量到 Cloudflare Pages 的 Functions 环境变量设置中

# ===========================================
# 基础配置 (必需)
# ===========================================
SKLAND_TOKEN=your_skland_token_here

# ===========================================
# ServerChan 通知配置 (可选)
# ===========================================
SERVERCHAN_SENDKEY=your_serverchan_sendkey_here

# ===========================================
# SMTP 邮件配置 (可选)
# 注意：需要 HTTP-to-SMTP 代理服务
# ===========================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_TLS=true
SMTP_USER=your_email@example.com
SMTP_PASS=your_password_or_api_key
EMAIL_FROM=sender@example.com
EMAIL_TO=recipient@example.com

# ===========================================
# 钉钉机器人通知 (可选)
# ===========================================
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=your_token

# ===========================================
# 企业微信机器人通知 (可选)
# ===========================================
WECHAT_WORK_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key

# ===========================================
# Slack 通知 (可选)
# ===========================================
SLACK_WEBHOOK=https://hooks.slack.com/services/your/slack/webhook

# ===========================================
# 通用 Webhook 通知 (可选)
# ===========================================
WEBHOOK_URL=https://your-webhook-endpoint.com/notify

# ===========================================
# Bark 推送通知 (可选)
# ===========================================
BARK_URL=https://api.day.app/your_bark_key